# Scansca Configuration

# HTTP server settings
http:
  port: 8080

# Configuration directory
config:
  dir: "./config"

# Database settings
databases:
  # Example PostgreSQL database connection (uncomment to use)
  # postgres-example:
  #   type: "postgresql" 
  #   connection_string: "postgres://scansca_user:scansca_password@localhost:5432/scansca"